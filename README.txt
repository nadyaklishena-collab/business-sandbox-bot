
Business Sandbox Aarhus — Registration Bot (v3)
===============================================

Цей бот реєструє учасниць Business Sandbox Aarhus, показує коротке вітальне повідомлення,
пояснює обробку персональних даних, запитує згоду, пропонує вибір мови
(українська / російська) і зберігає дані в Google Sheets.

Основний сценарій
-----------------

1. /start
   - Вітальне повідомлення про Business Sandbox та кнопку «Старт».

2. Після натискання «Старт»
   - Коротке пояснення про обробку персональних даних.
   - Лінк на політику конфіденційності (Google Docs).

3. Користувач обирає:
   - «Погоджуюсь» → перехід до вибору мови.
   - «Не погоджуюсь» → повідомлення та кнопка «Старт» для можливого повторного запуску.

4. Вибір мови:
   - 🇺🇦 Українська
   - 🇷🇺 Русский

5. Опитувальник (ім'я, телефон, місто, сфера, досвід) на обраній мові.

6. Телефон:
   - Кнопка «Поділитися контактом» / «Поделиться контактом» (Telegram-номер).
   - Можливість ввести номер вручну у форматі +45 00 00 00 00.
   - Є проста перевірка коректності номера.

7. Дані зберігаються в Google Sheets:
   - timestamp, user_id, username, first_name, segment, name, phone, city, field, experience, source, lang.

8. Фінальне повідомлення:
   - Подяка за реєстрацію.
   - Обіцянка зв'язатися у березні (або раніше, якщо строки зміняться).

Файли
-----

- main.py          — основний код бота
- .env.example     — приклад файлу налаштувань
- requirements.txt — список Python-залежностей
- creds.json       — файл сервіс-акаунта Google (Ти додаєш сама, в архів не входить)

Налаштування
------------

1. Створи віртуальне середовище (за бажанням):

   python -m venv venv
   venv\Scripts\activate  (Windows)
   source venv/bin/activate (Mac/Linux)

2. Встанови залежності:

   py -m pip install -r requirements.txt

3. Створи файл .env на основі .env.example:

   - TELEGRAM_BOT_TOKEN — токен твого Telegram-бота (від BotFather)
   - SPREADSHEET_ID — ID таблиці Google Sheets (частина URL між /d/ та /edit)
   - WORKSHEET_NAME — назва аркуша (вкладки) всередині таблиці, наприклад: Registrations

4. Поклади файл сервіс-акаунта Google:

   - Створи сервіс-акаунт у Google Cloud Console
   - Ввімкни Google Sheets API
   - Завантаж JSON-файл і назви його `creds.json`
   - Поклади його в папку з main.py

5. Видай доступ сервіс-акаунту до таблиці:

   - Відкрий Google Sheets
   - Натисни "Поделиться / Share"
   - Додай email сервіс-акаунта з `creds.json` як Editor (Редактор)

Запуск
------

   py -3.10 main.py

або, якщо використовується просто python:

   python main.py

